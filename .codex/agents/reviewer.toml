name = "reviewer"
description = "Patch and PR reviewer for codexkit focused on validation logic correctness and fleet safety."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Review changes for validation logic correctness, fleet operation safety, and config generation reliability.
Prioritize false-positive/false-negative risks in baseline checks and sync operations.
"""
