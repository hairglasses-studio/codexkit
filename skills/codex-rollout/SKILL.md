---
name: codex-rollout
description: Fleet-wide Codex migration loop — baseline validation, skill-surface sync, workflow audit, and compatibility docs across all repos.
allowed-tools:
  - Bash
  - Read
  - Write
  - Grep
  - Glob
---

<!-- GENERATED BY hg-global-skill-sync.sh FROM ~/.claude/skills/codex-rollout/SKILL.md; DO NOT EDIT -->

# Codex Rollout

Migrate repos from Claude-first state to the current Codex baseline. Works from any repo or from the org root.

## Default loop

1. **Classify** the repo first: operator (Tier 1), active Tier 2, or exempt.
2. **Establish baseline** before deeper enhancements:
   - `AGENTS.md` — canonical instruction file
   - Compatibility docs — CLAUDE.md, GEMINI.md, .github/copilot-instructions.md
   - `.codex/config.toml` — Codex-specific config
   - Live Codex workflows verified
3. **Skill surface**: if the repo has skills, move them to `.agents/skills/` and generate compatibility outputs:
   ```bash
   ~/dotfiles/scripts/hg-skill-surface-sync.sh
   ```
4. **Use shared scripts** for org-wide changes:
   - `hg-workflow-sync.sh` — sync CI workflows
   - `hg-codex-audit.sh` — audit Codex readiness
   - `hg-codex-mcp-sync.sh` — sync MCP config to Codex format
5. **Verify and audit** — end with evidence-backed verification.

## Working rules

- Prefer shared scripts over one-off edits when the change is org-wide
- Keep provider-neutral repo tooling provider-neutral (no Claude-only defaults where a shared path exists)
- State whether a change is repo-local, workspace-local, or user-global
