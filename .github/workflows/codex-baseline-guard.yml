name: "Deprecated: Codex Baseline Guard"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Local baseline is authoritative
        run: echo "Deprecated diagnostic only. Run 'make baseline' in a local checkout."
