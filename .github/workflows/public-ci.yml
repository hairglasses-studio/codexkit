name: "Deprecated: Public CI"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Local CI is authoritative
        run: echo "Deprecated diagnostic only. Run 'make ci' in a local checkout."
