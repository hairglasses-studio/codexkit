name: "Deprecated: Dependabot Auto-Merge"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Automation is disabled
        run: echo "Deprecated diagnostic only. No pull request mutation or auto-merge runs here."
