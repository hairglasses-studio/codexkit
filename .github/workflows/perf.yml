name: "Deprecated: Perf"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Local benchmarks are authoritative
        run: echo "Deprecated diagnostic only. Run Go benchmarks locally when needed."
